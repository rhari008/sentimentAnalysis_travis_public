'use strict';

var winstonUtils = require('./winston-utils');

module.exports = File;

function File(filepath) {
  this.winston = winstonUtils.createFileLogger(filepath);
  this.log = winstonUtils.log.bind(this.winston);
}
