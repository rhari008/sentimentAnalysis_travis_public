'use strict';

var winston = require('winston');

module.exports.getLogger = function () {
  var logger = new (winston.Logger)({
    transports : [ new (winston.transports.Console)() ]
  });
  logger.level = process.env.SAP_HDBEXT_LOGLEVEL || 'error';
  return logger;
};
